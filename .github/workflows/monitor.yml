name: TestFlight Monitor - WhatsApp Business Beta

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Rodar monitor
        run: python3 monitor.py
